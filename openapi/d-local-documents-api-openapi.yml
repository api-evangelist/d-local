openapi: 3.2.0
info:
  title: dLocal KYC Verifications Documents API
  version: '1.0'
  description: 'Create and manage KYC verifications across payment flows: remittances,

    payouts on hold, payins on hold, and standalone verifications.

    Verification results are delivered asynchronously via webhook.

    '
servers:
- url: https://api.dlocal.com
  description: Production
- url: https://sandbox.dlocal.com
  description: Sandbox
security:
- dLocalSignature: []
tags:
- name: Documents
  description: Upload and retrieve verification documents.
paths:
  /verifications/{verification_id}/documents:
    get:
      tags:
      - Documents
      operationId: getVerificationDocuments
      summary: Get Verification Documents
      parameters:
      - name: verification_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Documents returned
  /verifications/{verification_id}/documents/{document_id}:
    patch:
      tags:
      - Documents
      operationId: updateVerificationDocument
      summary: Update A Verification Document
      parameters:
      - name: verification_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: document_id
        in: path
        required: true
        schema:
          type: string
      requestBody:
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Document updated
components:
  securitySchemes:
    dLocalSignature:
      type: apiKey
      in: header
      name: Authorization
